package net.donutsmp.balance.client.config;

/**
 * One row in your personal registry (which in-game account a key belongs to).
 * Stored only in {@code config/donutsmp-balance.json} on this machine.
 */
public class KeyRegistryEntry {
    public String username = "";
    public String apiKey = "";
}
